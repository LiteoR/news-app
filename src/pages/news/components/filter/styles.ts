import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginHorizontal: 16,
    marginBottom: 16,
    flexDirection: 'column',
  },
  row: {
    flexDirection: 'row',
    marginBottom: 8,
  },
  gap: {
    marginRight: 8,
  },
});
